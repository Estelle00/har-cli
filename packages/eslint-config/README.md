# `eslint-config`
eslint 配置文件

## 快速上手
```
npm安装：npm i eslint prettier @har/eslint-config -D
yarn安装：yarn add eslint prettier @har/eslint-config -D
```

```
添加如下代码到package.json
"eslintConfig": {
  "root": true,
  "extends": [
    "@har"
  ]
}
```
